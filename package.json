{
	"name": "agora",
	"version": "0.0.1",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev:engine": "cd apps/engine && uv run uvicorn app.main:app --reload --port 8000",
		"dev:arena": "cd apps/arena && bun run dev",
		"dev": "concurrently \"bun run dev:engine\" \"bun run dev:arena\""
	},
	"devDependencies": {
		"@types/bun": "^1.3.11",
		"concurrently": "^9.2.1"
	},
	"peerDependencies": {
		"typescript": "^6.0.2"
	}
}
